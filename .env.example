# Copy to .env and fill in your values. Do not commit .env.

# Insforge – REQUIRED for login, signup, and Intelligent Claim Builder (document analysis).
# Get your anon key from the Insforge project dashboard (Settings / API Keys).
# Key format is usually ik_... (paste the full key with no spaces).
INSFORGE_ANON_KEY=your_anon_key_from_insforge_dashboard

# Optional: override backend URL. Default below is the current project.
# INSFORGE_API_BASE_URL=https://2ri79ay3.us-west.insforge.app
INSFORGE_API_BASE_URL=https://2ri79ay3.us-west.insforge.app

# Optional: dev server bind address. Use 0.0.0.0 to allow browser from localhost or 127.0.0.1.
# HOST=0.0.0.0

# Stripe (https://dashboard.stripe.com/apikeys)
STRIPE_PUBLISHABLE_KEY=pk_live_xxx
STRIPE_SECRET_KEY=sk_live_xxx

# Optional: for subscription sync after payment
# STRIPE_WEBHOOK_SECRET=whsec_xxx
