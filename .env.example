# Copy to .env and fill in your values. Do not commit .env.
# App runs at https://vaclaimnavigator.com. Set VITE_API_URL if your backend is elsewhere.

# Frontend (Vite): API base URL for YOUR backend. Omit for same-origin. Do not use for Insforge.
VITE_API_URL=https://vaclaimnavigator.com

# Frontend (Vite): optional – if set, login page won't show "auth not configured" when server env is wrong.
# Insforge base URL must come from VITE_INSFORGE_API_BASE_URL (not VITE_API_URL).
# VITE_INSFORGE_ANON_KEY=your_anon_key_from_insforge_dashboard
# VITE_INSFORGE_API_BASE_URL=https://2ri79ay3.us-west.insforge.app

# Insforge – REQUIRED for login, signup, and Intelligent Claim Builder (document analysis).
# Get your anon key from the Insforge project dashboard (Settings / API Keys).
# Key format is usually ik_... (paste the full key with no spaces).
INSFORGE_ANON_KEY=your_anon_key_from_insforge_dashboard

# Optional: override backend URL. Default below is the current project.
# INSFORGE_API_BASE_URL=https://2ri79ay3.us-west.insforge.app
INSFORGE_API_BASE_URL=https://2ri79ay3.us-west.insforge.app

# Optional: backend bind address (default 0.0.0.0). APP_URL for redirects (default https://vaclaimnavigator.com).
# HOST=0.0.0.0
# APP_URL=https://vaclaimnavigator.com

# Stripe (https://dashboard.stripe.com/apikeys)
STRIPE_PUBLISHABLE_KEY=pk_live_xxx
STRIPE_SECRET_KEY=sk_live_xxx

# Optional: for subscription sync after payment
# STRIPE_WEBHOOK_SECRET=whsec_xxx
