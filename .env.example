# Copy to .env and fill in your values. Do not commit .env.

# ─── Insforge (REQUIRED) ─────────────────────────────────────────────────────
# Required for login, signup, database, storage, and AI (Intelligent Claim Builder).
# Get your anon key from the Insforge project dashboard (Settings / API Keys).
INSFORGE_ANON_KEY=your_anon_key_from_insforge_dashboard
INSFORGE_API_BASE_URL=https://2ri79ay3.us-west.insforge.app

# ─── Database ─────────────────────────────────────────────────────────────────
# NOT required at runtime — the app uses Insforge SDK for all database access.
# Only needed for Drizzle migrations (npm run db:push).
# DATABASE_URL=postgresql://user:pass@host:5432/dbname

# ─── Stripe (https://dashboard.stripe.com/apikeys) ───────────────────────────
STRIPE_PUBLISHABLE_KEY=pk_live_xxx
STRIPE_SECRET_KEY=sk_live_xxx
# STRIPE_WEBHOOK_SECRET=whsec_xxx
# STRIPE_PRICE_ID_PRO=price_xxx
# STRIPE_PRICE_ID_DELUXE=price_xxx
# STRIPE_PRICE_ID_BUSINESS=price_xxx

# ─── Vercel (auto-set by Vercel, do not set manually) ────────────────────────
# VERCEL_URL is automatically provided by Vercel at deploy time
